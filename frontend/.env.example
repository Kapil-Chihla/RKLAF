# Public API URL (no trailing slash). Set in Vercel → Project → Settings → Environment Variables.
# Example after deploying backend on Render:
# VITE_API_BASE_URL=https://legal-aid-api.onrender.com/api

VITE_API_BASE_URL=http://localhost:5000/api
